var board = [];
var score = 0;
function newGame(){
	//init();
	generatenumber();
}
// function init(){

// }
function generatenumber(){
	var randomNumber = parseInt(Math.floor(Math.random() * 16));
	console.log(randomNumber);
}